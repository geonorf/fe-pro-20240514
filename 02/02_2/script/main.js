// Ask value
let value = prompt("Enter number, please");

let ResValue = value%2;

// Print result
if (ResValue === 0)
    console.log(`Result: ${ResValue}. Value is even.`);
else
    console.log(` Reesult: ${ResValue}. Value is odd.`);