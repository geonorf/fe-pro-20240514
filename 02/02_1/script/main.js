// Set values
let number1 = 149;
let number2 = 150;

// Compare
if (number1 == number2)
    alert(`Number1 (${number1}) equal to Number2 (${number2})`);
else
    alert(`Number1 (${number1}) not equal to Number2 (${number2})`);
